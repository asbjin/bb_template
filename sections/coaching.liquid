{% comment %}
  Floreva — Section Premium : "Coaching avec Spécialiste"
  Prérequis : UNIQUEMENT bundle 150 jours, coaching 100% par messages (pas d'appel/vidéo).
  Mobile-first, style premium avec accents dorés. Pas de FAQ ici : utilisez la section FAQ du thème.
{% endcomment %}
<section class="flv-coach" style="--green: {{ section.settings.color_primary }}; --gold: {{ section.settings.color_gold }}; --ink:#111; --bg:#fff;">
  <div class="flv-wrap">

    <!-- EN-TÊTE -->
    <header class="flv-hero">
      <div class="flv-badges">
        <span class="flv-chip flv-chip--gold">OFFERT 3 mois</span>
        <span class="flv-chip">UNIQUEMENT bundle 150 jours</span>
        <span class="flv-chip">100% par messages</span>
      </div>

      {% assign fallback_url = 'Kaching-Bundles-ChatGPT_Image_15_oct._2025_23_52_00.png' | file_url %}
      <img class="flv-hero-icon"
          src="{% if section.settings.hero_icon != blank %}{{ section.settings.hero_icon | image_url: width: 192 }}{% else %}{{ fallback_url }}{% endif %}"
          alt="{{ section.settings.hero_alt | escape }}"
          loading="lazy">

      <h2 class="flv-title">{{ section.settings.title }}</h2>
      <p class="flv-sub">{{ section.settings.subtitle }}</p>

      <div class="flv-cta">
        {% if section.settings.bundle_url != blank %}
          <a class="flv-btn flv-btn--solid" href="{{ section.settings.bundle_url }}">
            {{ section.settings.cta_bundle }}
          </a>
        {% endif %}
        {% if section.settings.whatsapp_url != blank %}
          <a class="flv-btn flv-btn--gold" href="{{ section.settings.whatsapp_url }}" target="_blank" rel="noopener nofollow">
            {{ section.settings.cta_whatsapp }}
          </a>
        {% endif %}
      </div>
      <p class="flv-note">Pas d'appels ni de vidéos : un accompagnement rapide, discret et quand vous en avez besoin.</p>
    </header>

    <!-- AVANTAGES -->
    <section class="flv-block flv-points">
      <h3 class="flv-h3">{{ section.settings.usp_title }}</h3>
      <ul class="flv-list">
        <li>💬 <strong>Toujours à portée de main :</strong> écrivez-nous quand vous voulez — pas d'horaires fixes.</li>
        <li>🧭 <strong>Conseils personnalisés :</strong> routine, bases alimentaires et mode de vie adaptés à votre profil.</li>
        <li>📈 <strong>Objectif résultats :</strong> mini-bilans réguliers pour optimiser vos habitudes et vos progrès.</li>
      </ul>
    </section>

    <!-- ÉTAPES interactives (CSS uniquement) -->
    <section class="flv-block flv-steps">
      <h3 class="flv-h3">{{ section.settings.steps_title }}</h3>

      <div class="flv-stepper">
        <input type="radio" name="flv-step" id="flv-s1" checked>
        <input type="radio" name="flv-step" id="flv-s2">
        <input type="radio" name="flv-step" id="flv-s3">

        <div class="flv-step-tabs" aria-label="Progression">
          <label for="flv-s1">1</label>
          <label for="flv-s2">2</label>
          <label for="flv-s3">3</label>
          <span class="flv-progress"></span>
        </div>

        <div class="flv-step-panels">
          <article class="flv-panel">
            <h4>Achetez le bundle 150 jours</h4>
            <p>Le coaching offert est réservé <strong>uniquement</strong> au pack 150 jours.</p>
            {% if section.settings.bundle_url != blank %}
              <a href="{{ section.settings.bundle_url }}" class="flv-link">Voir le bundle</a>
            {% endif %}
          </article>
          <article class="flv-panel">
            <h4>Démarrage du coaching : e-mail d'instructions + chat</h4>
            <p>Objectifs, journal initial et plan de départ personnalisé — directement par messagerie.</p>
            <p class="flv-hint">Nous vous enverrons un e-mail avec les détails pour contacter notre service de coaching.</p>
          </article>
          <article class="flv-panel">
            <h4>Suivi pendant 3 mois</h4>
            <p>Messages illimités (5j/7) + <strong>2 bilans mensuels</strong> en chat pour optimiser doses, habitudes et progrès.</p>
          </article>
        </div>
      </div>
    </section>

    <!-- CE QUI EST INCLUS -->
    <section class="flv-block flv-includes">
      <h3 class="flv-h3">{{ section.settings.includes_title }}</h3>
      <div class="flv-grid flv-grid--2">
        {% for block in section.blocks %}
          {% if block.type == 'service' %}
            <article class="flv-card flv-card--lift">
              {% if block.settings.icon != blank %}
                <img class="flv-ico" src="{{ block.settings.icon | image_url: width: 128 }}" alt="" loading="lazy">
              {% endif %}
              <h4 class="flv-h4">{{ block.settings.heading }}</h4>
              <p class="flv-p">{{ block.settings.text }}</p>
            </article>
          {% endif %}
        {% endfor %}
      </div>
    </section>

    <!-- PROGRAMME -->
    <section class="flv-block flv-roadmap">
      <h3 class="flv-h3">{{ section.settings.roadmap_title }}</h3>
      <div class="flv-road">
        <div class="flv-stage">
          <div class="flv-badge">1</div>
          <h5>Mois 1 — Remise à zéro & Routine</h5>
          <p>Lancement, routine de base, hydratation, timing des repas, gestion des ballonnements.</p>
        </div>
        <div class="flv-stage">
          <div class="flv-badge">2</div>
          <h5>Mois 2 — Optimisation</h5>
          <p>Ajustement des doses, focus peau/acné, combinaisons alimentaires et mode de vie.</p>
        </div>
        <div class="flv-stage">
          <div class="flv-badge">3</div>
          <h5>Mois 3 — Consolidation</h5>
          <p>Stabilisation des habitudes, plan de maintien, bilan final.</p>
        </div>
      </div>
    </section>

    <!-- OFFRE / BOUTONS -->
    <section class="flv-offer">
      <div class="flv-offer-inner">
        <p class="flv-offer-kicker">OFFERT pendant 3 mois</p>
        <h3 class="flv-h3">Coaching avec Spécialiste — valeur {{ section.settings.value_anchor }}</h3>
        <p class="flv-p">Réservé exclusivement au <strong>bundle 150 jours</strong>. Places limitées chaque mois pour garantir la qualité de l'accompagnement.</p>
        <div class="flv-cta">
          {% if section.settings.bundle_url != blank %}
            <a class="flv-btn flv-btn--solid" href="{{ section.settings.bundle_url }}">{{ section.settings.cta_bundle }}</a>
          {% endif %}
          {% if section.settings.whatsapp_url != blank %}
            <a class="flv-btn flv-btn--gold" href="{{ section.settings.whatsapp_url }}" target="_blank" rel="noopener nofollow">{{ section.settings.cta_whatsapp }}</a>
          {% endif %}
        </div>
      </div>
      <p class="flv-legal">Le coaching ne remplace pas un avis médical.</p>
    </section>

    <!-- Pas de FAQ ici : utilisez la section FAQ du thème -->
  </div>

  <style>
    /* ====== MOBILE EN PRIORITÉ / PREMIUM ====== */
    .flv-coach{background:var(--bg);color:var(--ink);}
    .flv-wrap{max-width:{{ section.settings.max_width }};margin:0 auto;padding:20px 16px;}

    .flv-hero{position:relative;background:linear-gradient(180deg, rgba(215,169,75,.10), transparent);
      border:1px solid rgba(215,169,75,.35);border-radius:18px;padding:16px;
      box-shadow:0 10px 28px rgba(215,169,75,.14);}
    .flv-badges{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:8px}
    .flv-chip{display:inline-flex;align-items:center;padding:6px 10px;border-radius:999px;
      background:#F7F1E3;border:1px solid rgba(215,169,75,.6);font-size:12px;font-weight:700}
    .flv-chip--gold{background:linear-gradient(180deg, #F6E4B6, #EFD28B)}
    .flv-hero-icon{width:72px;height:72px;object-fit:contain;display:block;margin-bottom:8px;filter:drop-shadow(0 1px 0 rgba(0,0,0,.05));}
    .flv-title{font-size:28px;line-height:1.2;margin:8px 0 6px;}
    .flv-sub{font-size:16px;opacity:.9;margin:0 auto 6px;}
    .flv-note{font-size:13px;opacity:.85;margin-top:8px}
    .flv-cta{display:flex;gap:10px;flex-wrap:wrap;margin-top:10px}
    .flv-btn{display:inline-block;padding:12px 16px;border-radius:14px;text-decoration:none;font-weight:700}
    .flv-btn--solid{background:var(--green);color:#fff;box-shadow:0 6px 20px rgba(0,126,64,.25)}
    .flv-btn--gold{background:linear-gradient(180deg, var(--gold), #C99638);color:#1A1A1A}
    .flv-link{color:var(--green);font-weight:700;text-decoration:underline}

    .flv-block{margin-top:22px}
    .flv-h3{font-size:20px;margin:0 0 8px}
    .flv-h4{font-size:17px;margin:6px 0}
    .flv-p{font-size:15px;margin:0}
    .flv-list{padding-left:18px;margin:0}
    .flv-list li{margin:6px 0}

    /* Étapes */
    .flv-stepper{background:#fff;border:1px solid #eee;border-radius:16px;padding:12px}
    .flv-stepper input{display:none}
    .flv-step-tabs{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;position:relative;margin-bottom:10px}
    .flv-step-tabs label{display:grid;place-items:center;height:34px;border-radius:10px;
      border:1px solid #eee;background:#fafafa;font-weight:800}
    .flv-progress{position:absolute;left:0;top:0;height:34px;width:33.33%;border-radius:10px;
      background:linear-gradient(180deg, var(--gold), #C99638);z-index:0;transition:transform .25s ease}
    #flv-s1:checked ~ .flv-step-tabs .flv-progress{transform:translateX(0)}
    #flv-s2:checked ~ .flv-step-tabs .flv-progress{transform:translateX(100%)}
    #flv-s3:checked ~ .flv-step-tabs .flv-progress{transform:translateX(200%)}
    .flv-step-tabs label{position:relative;z-index:1}

    .flv-step-panels{display:grid}
    .flv-panel{padding:8px 4px}
    #flv-s1:checked ~ .flv-step-panels .flv-panel:nth-child(1){display:block}
    #flv-s2:checked ~ .flv-step-panels .flv-panel:nth-child(2){display:block}
    #flv-s3:checked ~ .flv-step-panels .flv-panel:nth-child(3){display:block}
    .flv-step-panels .flv-panel{display:none}

    /* Cartes & grille */
    .flv-grid{display:grid;gap:12px}
    .flv-grid--2{grid-template-columns:1fr}
    .flv-card{background:#FAFAFA;border:1px solid #EEE;border-radius:16px;padding:14px}
    .flv-card--lift{transition:transform .15s ease, box-shadow .15s ease}
    .flv-card--lift:hover{transform:translateY(-2px);box-shadow:0 10px 24px rgba(0,0,0,.06)}
    .flv-ico{width:56px;height:56px;object-fit:contain}

    /* Programme */
    .flv-road{display:flex;gap:10px;flex-direction:column}
    .flv-stage{position:relative;background:#fff;border:1px solid #EEE;border-radius:14px;padding:12px 12px 12px 48px;box-shadow:0 8px 24px rgba(0,0,0,.04)}
    .flv-badge{position:absolute;left:10px;top:10px;width:28px;height:28px;border-radius:50%;background:var(--gold);color:#1A1A1A;font-weight:800;display:grid;place-items:center}

    /* Offre */
    .flv-offer{margin-top:24px}
    .flv-offer-inner{border-radius:18px;padding:16px;background:linear-gradient(180deg, rgba(215,169,75,.18), rgba(215,169,75,.06));border:1px solid rgba(215,169,75,.45)}
    .flv-offer-kicker{font-weight:800;letter-spacing:.2px;color:#1A1A1A;margin:0 0 4px}
    .flv-legal{font-size:12px;opacity:.8;margin-top:8px;text-align:center}

    /* Ajustements bureau */
    @media (min-width: 768px){
      .flv-title{font-size:34px}
      .flv-sub{font-size:18px;max-width:680px}
      .flv-grid--2{grid-template-columns:repeat(2,1fr)}
      .flv-road{flex-direction:row}
      .flv-stage{flex:1}
    }
  </style>
</section>

{% schema %}
{
  "name": "Coaching",
  "settings": [
    { "type": "color", "id": "color_primary", "label": "Couleur primaire (vert)", "default": "#007E40" },
    { "type": "color", "id": "color_gold", "label": "Accent doré", "default": "#D7A94B" },
    { "type": "image_picker", "id": "hero_icon", "label": "Icône en-tête (PNG/SVG transparent)" },
    { "type": "text", "id": "hero_alt", "label": "Texte alternatif icône", "default": "Icône coaching Floreva" },

    { "type": "text", "id": "title", "label": "Titre", "default": "Coaching avec Spécialiste — OFFERT pendant 3 mois" },
    { "type": "textarea", "id": "subtitle", "label": "Sous-titre", "default": "Accompagnement 100% par messages sur la peau/acné, la digestion et l'énergie pour maximiser vos résultats avec Floreva." },

    { "type": "url", "id": "bundle_url", "label": "Lien page bundle 150 jours" },
    { "type": "text", "id": "cta_bundle", "label": "Texte bouton bundle", "default": "Activer le bundle 150 jours" },

    { "type": "url", "id": "whatsapp_url", "label": "Lien WhatsApp (wa.me/...)" },
    { "type": "text", "id": "cta_whatsapp", "label": "Texte bouton WhatsApp", "default": "Écrivez-nous sur WhatsApp" },

    { "type": "text", "id": "usp_title", "label": "Titre section Pourquoi c'est différent", "default": "Pourquoi c'est différent" },
    { "type": "text", "id": "steps_title", "label": "Titre section Comment ça marche", "default": "Comment ça marche" },
    { "type": "text", "id": "includes_title", "label": "Titre section Ce qui est inclus", "default": "Ce qui est inclus (3 mois)" },
    { "type": "text", "id": "roadmap_title", "label": "Titre section Programme", "default": "Programme des 3 mois" },

    { "type": "text", "id": "value_anchor", "label": "Valeur affichée (ex. 149€)", "default": "149€" },
    { "type": "text", "id": "max_width", "label": "Largeur maximale conteneur", "default": "760px" }
  ],
  "blocks": [
    {
      "type": "service",
      "name": "Service inclus",
      "settings": [
        { "type": "image_picker", "id": "icon", "label": "Icône" },
        { "type": "text", "id": "heading", "label": "Titre", "default": "Bilan initial en chat (20–30 min)" },
        { "type": "textarea", "id": "text", "label": "Texte", "default": "Objectifs, journal initial, plan de départ personnalisé." }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [{ "name": "Coaching avec Spécialiste (Premium)" }]
}
{% endschema %}
